# git_tutorial3


```
echo '# title' >> README.md

git init

git add . (. = all file)

git commit -m 'comment' -- 

git branch -m main  -- if no branch, creates a new branch named <main>




git log --graph --oneline



```
